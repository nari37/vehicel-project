{
    "version": 2,
    "builds": [
      {
        "src": "server/server.js",
        "use": "@vercel/node"
      },
      {
        "src": "client/static/**/*",
        "use": "@vercel/static-build",
        "config": { "distDir": "client/static" }
      }
    ],
    "routes": [
      { "src": "/api/(.*)", "dest": "server/server.js" },
      { "src": "/static/(.*)", "dest": "/client/static/$1" },
      { "src": "/(.*)", "dest": "/client/index.html" }
    ]
  }
  